"use client";
import {useEffect,useState} from "react";
import "./market.css";
type Item={label:string;value:string;note:string};
export function MarketPanel(){const[items,setItems]=useState<Item[]>([]);useEffect(()=>{Promise.all([fetch("https://api.frankfurter.dev/v2/rates?base=USD&quotes=TRY,EUR,GBP").then(r=>r.json()),fetch("https://api.gold-api.com/price/XAU").then(r=>r.json())]).then(([fx,gold])=>{const rate=(q:string)=>fx.find((x:{quote:string;rate:number})=>x.quote===q).rate,usd=rate("TRY"),fmt=(v:number,s="₺")=>new Intl.NumberFormat("tr-TR",{maximumFractionDigits:2,minimumFractionDigits:2}).format(v)+" "+s;setItems([{label:"USD / TL",value:fmt(usd),note:"1 Amerikan Doları"},{label:"EUR / TL",value:fmt(usd/rate("EUR")),note:"1 Euro"},{label:"GBP / TL",value:fmt(usd/rate("GBP")),note:"1 İngiliz Sterlini"},{label:"GRAM ALTIN",value:fmt(gold.price*usd/31.1034768),note:"Spot hesaplama"},{label:"ONS ALTIN",value:fmt(gold.price,"$"),note:"XAU / USD"}])}).catch(()=>{})},[]);return <section className="market"><div className="market-head"><h2>Piyasa özeti</h2><span>GÜNCEL REFERANS DEĞERLER</span></div><div className="market-grid">{items.length?items.map(x=><div className="market-card" key={x.label}><small>{x.label}</small><strong>{x.value}</strong><span>{x.note}</span></div>):<div className="market-card"><small>PİYASA VERİLERİ</small><strong>Yükleniyor…</strong></div>}</div><p className="market-note">Değerler bilgi amaçlıdır; alım-satım tavsiyesi değildir.</p></section>}
